# Optional interactive notebooks

The production workflow is in `scripts/`. In OnDemand/Jupyter, use the same `spatialdata` kernel and import functions from `src/` rather than copying helper code into notebooks. The figures/tables generated by scripts are intended to guide interactive follow-up.
